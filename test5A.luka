// Test 5 for the luka coding language
// Testing comparison operators (==, !=, >, <, >=, <=)

// Test that we can deal with them like normal booleans
print( 5 == 5 );
val no: bool = 5 == 3;

// Make sure the spacing doesn't matter
val b =1==1;
val b =1!=1;
val b =1>1;
val b =1<1;
val b =1>=1;
val b =1<=1;

// Make sure it doesn't allow broken assignments
// val wrong == 5;
// val ew >= 5;
// val bad != 5;

// Test if they give the correct result
// Equals
print( 1 == 1 );
print( 1 == 2 );

// Not Equals
print( 1 != 2 );
print( 1 != 1 );

// Greater than
print( 2 > 1 );
print( 1 > 1 );
print( 1 > 2 );

// Less than
print( 1 < 2 );
print( 1 < 1 );
print( 2 < 1 );

// Greater or equal
print( 2 >= 1);
print( 1 >= 1);
print( 1 >= 2);

// Less or equal
print( 1 <= 2 );
print( 1 <= 1 );
print( 2 <= 1 );

// testing them all with variables rather than immediates
val x: int = 1;
val y: int = 2;

val boolean = x == y;
val boolean = x != y;
val boolean = x > y;
val boolean = x < y;
val boolean = x >= y;
val boolean = x <= y;